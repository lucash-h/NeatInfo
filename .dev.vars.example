PASSPHRASE=neatinfo-dev
SESSION_SECRET=dev-only-not-a-real-secret-change-me

# Note: Workers AI has no local mode. `wrangler dev` calls the real remote
# model and spends real neurons (10,000/day are free), so listening to a long
# article against a dev server costs the same as against production.
